@model IEnumerable<SistemaReservasViajes.Models.Actividad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReservaID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DestiniID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreActividad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaActividad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DuracionHoras)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReservaID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DestiniID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreActividad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaActividad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DuracionHoras)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ActividadID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ActividadID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ActividadID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
